<system>
  workers 4     # worker parameter
</system>

# work | MULTI process workers,      worker0 - worker3 run in_forward
<source>
  @type forward
</source>

# work | ONLY worker 0      ==  worker1 - worker3 do NOT run | in_tail
<worker 0>
  <source>
    @type tail
  </source>
</worker>
